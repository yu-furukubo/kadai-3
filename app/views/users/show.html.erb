<%= render 'layouts/sidebar',user: @user %>

<h1>Books</h1>
<table>
  <tr>
    <th>Title</th>
    <th>Opinion</th>
  </tr>

  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <!--<td><%= link_to 'Show', user_path(@user.id) %></td>-->
    </tr>
  <% end %>
</table>